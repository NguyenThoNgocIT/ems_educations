server:
  port: 8081
spring:
  datasource:
    # công thêm database online
    url: jdbc:postgresql://ep-proud-lake-amungl4h-pooler.c-5.us-east-1.aws.neon.tech/ems_db?sslmode=require
    username: neondb_owner
    password: npg_l0GWrf2NEHRT
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update   # allow schema update for new fields
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    
application:
  security:
    jwt:
      secret-key: mySecretKeyForJwtTokenGenerationWhichShouldBeAtLeast256BitsLongForSecurity123456789
      expiration: 86400000
      refresh-token:
        expiration: 604800000
